require "rails_helper"

RSpec.describe "deleting a workout", type: :feature do
  context "with no subscribers" do
    xit "will be successful" do
    end
  end

  context "with assigned plans" do
    xit "will throw an error" do
    end
  end
end
